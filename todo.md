Let inkgest remains the same, simple tool to get the content from URL


- Few change in app page, reduce the free credits per user only 10 and then change loalhost:3002 API backend endpoint for other tabs such as screenshot, blog, lanidng page generator and so on make the API endpoint similar to the scrape endpoint
- Then for each tab add one line description explaining this API or feature
- Lastly remove the credits bar circular chart aside to generate button 
